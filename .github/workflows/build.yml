name: Build RolBeheer
on: [push, workflow_dispatch]

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'
          cache: maven
      - run: mvn -B package
      - uses: actions/upload-artifact@v4
        with:
          name: RolBeheer
          path: target/RolBeheer-*.jar
      - name: Release maken
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          VERSION=$(mvn -q -DforceStdout help:evaluate -Dexpression=project.version)
          gh release delete "v$VERSION" -y --cleanup-tag || true
          gh release create "v$VERSION" target/RolBeheer-$VERSION.jar \
            --title "RolBeheer $VERSION" --notes "Automatisch gebouwd vanaf $GITHUB_SHA"
